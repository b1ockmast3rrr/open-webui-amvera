meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: ollama/ollama:latest
  command: null
  args: null
  persistenceMount: /root/.ollama
  containerPort: 11434
